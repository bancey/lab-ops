parameters:
  - name: addresses
    type: string

steps:
  - script: |
        sudo apt install ssh-tools
        nc -zv ${{ parameters.addresses }} 22
        ssh-ping ${{ parameters.addresses}}
#      if [[ "${{ parameters.addresses }}" == *","* ]]; then
#        OUTPUT=true
#        readarray -d , -t addressArr <<<"${{ parameters.addresses }}"
#        for (( n=0; n < ${#addressArr[*]}; n++ ))
#        do
#          if nc -z ${addressArr[n]} 22 2>/dev/null; then
#            echo "${addressArr[n]} ✓"
#          else
#            echo "${addressArr[n]} ✗"
#            OUTPUT=false
#          fi
#        done
#        if [[ "$OUTPUT" == "true" ]]; then
#          echo "##vso[task.setvariable variable=online;isOutput=true]true"
#        else
#          echo "##vso[task.setvariable variable=online;isOutput=true]false"
#        fi
#      else
#        if nc -z ${{ parameters.addresses }} 22 2>/dev/null; then
#          echo "${{ parameters.addresses }} ✓"
#          echo "##vso[task.setvariable variable=online;isOutput=true]true"
#        else
#          echo "${{ parameters.addresses }} ✗"
#          echo "##vso[task.setvariable variable=online;isOutput=true]false"
#        fi
#      fi
    name: checkOnline
