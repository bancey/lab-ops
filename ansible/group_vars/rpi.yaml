# Primary manager used for bootstrap and stack deployment delegation.
rpi_swarm_primary_manager: thanos

# Resolved across all rpi-ha.yaml plays, which each target hosts: rpi independently.
rpi_swarm_default_primary_manager: "{{ (groups['rpi_swarm_managers'] | default(groups['rpi']))[0] }}"
rpi_swarm_primary_manager_resolved: "{{ rpi_swarm_primary_manager | default(rpi_swarm_default_primary_manager) }}"

rpi_swarm_node_roles:
  thanos: manager
  nebula: manager
  gamora: manager

# Default stack names for docker stack deploy.
rpi_network_stack_name: rpi_network
rpi_monitoring_stack_name: rpi_monitoring

# Optional tuning knobs.
rpi_monitoring_adguard_exporter_replicas: 1

# Pinned per-host, not global/floating: gatus stores local sqlite history in
# /opt/gatus/data with no replication between nodes, so each instance needs to
# always land on the same host to keep its history. Matches the pre-swarm
# per-host compose setup (config already cross-links gatus.<host>.heimelska.co.uk).
rpi_monitoring_gatus_hosts:
  - thanos
  - gamora
