- name: Setup NUT client on wanda Proxmox host
  hosts: proxmox_wanda
  become: true
  become_user: root
  become_method: sudo
  gather_facts: true
  roles:
    - role: setup-nut-client
      nut_monitor_password: "{{ lookup('ansible.builtin.file', 'NUT-Monitor-Password') }}"