apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: unpoller
  namespace: monitoring
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 4.3.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      upoller:
        containers:
          upoller:
            image:
              repository: ghcr.io/unpoller/unpoller
              tag: v2.15.4
    persistence:
      upoller:
        type: secret
        name: upoller-config
        advancedMounts:
          upoller:
            upoller:
              - path: /etc/upoller/up.conf
                readOnly: true
                subPath: up.conf
