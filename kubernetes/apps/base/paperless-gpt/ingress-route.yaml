---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: paperless-gpt
  namespace: paperless-ngx
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`paperless-gpt.local`)
      kind: Rule
      services:
        - name: paperless-gpt
          port: 8080
      middlewares:
        - name: default-headers
          namespace: traefik
  tls:
    secretName: paperless-gpt-tls
