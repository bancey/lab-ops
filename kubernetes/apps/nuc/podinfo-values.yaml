apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  chart:
    spec:
      version: "6.3.0"
  values:
    ingress:
      enabled: true
      className: traefik
      hosts:
        - host: podinfo.nuc
          paths:
            - path: /
              pathType: ImplementationSpecific
