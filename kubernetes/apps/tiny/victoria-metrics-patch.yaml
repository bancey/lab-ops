---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoria-metrics
  namespace: monitoring
spec:
  values:
    server:
      extraArgs:
        finalMergeDelay: "1h"
        storage.maxHourlySeries: "50000"
      persistentVolume:
        storageClassName: unas-nfs-csi
      securityContext:
        runAsUser: 0
        runAsNonRoot: false
      ingress:
        enabled: true
        hosts:
          - name: "victoria-metrics.tiny.heimelska.co.uk"
            path: /
            port: http
        tls:
          - secretName: victoria-metrics-tls
            hosts:
              - "victoria-metrics.tiny.heimelska.co.uk"
