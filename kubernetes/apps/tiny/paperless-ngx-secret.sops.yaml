# NOTE: This file contains placeholder values that MUST be replaced
# with actual secrets and encrypted using SOPS before deployment.
# Run: sops -e -i kubernetes/apps/tiny/paperless-ngx-secret.sops.yaml
apiVersion: v1
kind: Secret
metadata:
  name: paperless-ngx-secret
  namespace: paperless-ngx
type: Opaque
stringData:
  secret-key: CHANGE_ME_PAPERLESS_SECRET_KEY
  postgres-password: CHANGE_ME_POSTGRES_PASSWORD
  redis-password: CHANGE_ME_REDIS_PASSWORD
